name: Go CI

on: [push]

jobs:
  build:
     runs-on: ubuntu-latest
     steps:
       -uses: actions/checkouts@v4
       -uses: actions/setup-go@v5
        with:
           go-version:"1.21"
       -run: go version
       